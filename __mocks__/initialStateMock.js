import { initialState as auth } from '../src/reducers/auth';
import { initialState as buildingProfile } from '../src/reducers/buildingProfile';
import { initialState as dms } from '../src/reducers/dms';
import { initialState as spNumbers } from '../src/reducers/spNumbers';

export const initialStateMock = {
  auth,
  buildingProfile,
  dms,
  spNumbers,
};
